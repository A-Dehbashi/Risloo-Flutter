import 'package:flutter/material.dart';

class FragmentAuthRegister extends StatelessWidget {
  const FragmentAuthRegister({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Register Page",
      ),
    );
  }

}