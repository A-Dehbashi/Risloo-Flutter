import 'package:flutter/material.dart';

class FragmentAuthPasswordRecover extends StatelessWidget {
  const FragmentAuthPasswordRecover({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Password Recover Page",
      ),
    );
  }

}